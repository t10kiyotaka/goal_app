<%= form_with model: model do |f| %>
  <%= f.text_area :body, class: "form-control", id: "new_comment_form" %>
  <br>
  <%= f.submit class: "btn btn-primary", data: { "disable-with": "Sending..." } %>
<% end %>
